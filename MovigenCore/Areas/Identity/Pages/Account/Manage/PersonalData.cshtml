@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Personal Data";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Su cuenta contiene datos personlas que nuestro sistema maneja.Este menú le permite descargar o eliminar estos datos.</p>
        <p>
            <strong class="text-danger">Eliminar estos datos removerá permanentemente su cuenta y no podrá ser recuperada.</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-default" type="submit">Descargar</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-danger">Eliminar</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}